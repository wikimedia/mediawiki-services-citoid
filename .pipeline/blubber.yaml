version: v4
base: docker-registry.wikimedia.org/nodejs24-slim
lives:
  in: /srv/service
runs:
  environment: { APP_BASE_PATH: /srv/service }

variants:
  build:
    base: docker-registry.wikimedia.org/nodejs24-slim
    copies: [local]
    apt: { packages: [git, python-is-python3, pkg-config, build-essential, ca-certificates] }
    node: { requirements: [package.json, package-lock.json]}
    runs: { environment: { LINK: g++, NODE_OPTIONS: --use-openssl-ca } }
    entrypoint: [npm, ci]
  development:
    includes: [build]
    entrypoint: [node, server.js]
  test:
    includes: [build]
    entrypoint: [npm, test]
    runs: { insecurely: true }
  prep:
    includes: [build]
    node: { env: production }
  production:
    copies: [prep]
    node: { env: production }
    apt: { packages: [ca-certificates] }
    runs: { environment: { NODE_OPTIONS: --use-openssl-ca } }
    entrypoint: [node, server.js]
